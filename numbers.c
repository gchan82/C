#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main(){
	/* comments */
	/* my program */
	int age;
	double gpa;
	char grade;
	char name[20];

	printf("Enter your name: ");
	fgets(name, 20, stdin);
	printf("Your name is %s", name);


	return 0;
}
